import { CurrencyValueColorPipe } from '../currency-value-color.pipe';

describe('CurrencyValueColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyValueColorPipe();
    expect(pipe).toBeTruthy();
  });
});
